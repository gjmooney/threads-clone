import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = async ({}) => {
  return (
    <section>
      <h1 className="head-text mb-10">communities</h1>
    </section>
  );
};

export default page;
